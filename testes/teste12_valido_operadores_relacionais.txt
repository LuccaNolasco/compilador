program comparacoes;
var x, y : integer;
begin
    x := 10;
    y := 20;
    if x < y then
        x := y;
    if x <= y then
        x := y;
    if x > y then
        y := x;
    if x >= y then
        y := x;
    if x = y then
        x := 0;
    if x <> y then
        x := 1;
end.

