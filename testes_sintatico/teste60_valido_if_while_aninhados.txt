program id;
var id, id : integer;
begin
    if id < num then
        while id > num do
            begin
                id := id - num;
                if id = num then
                    id := id + num;
            end;
end.

